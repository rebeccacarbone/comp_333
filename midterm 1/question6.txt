Consider the following JavaScript code:

function add(x, y) {
    return x + y;
}

function subtract(x, y) {
    return x - y;
}

let wrappedAdd = wrap(add);
let wrappedSubtract = wrap(subtract);

let temp1 = wrappedAdd(1, 2);
let temp2 = wrappedSubtract(6, 2);
console.log(temp1);
console.log(temp2);
This program has the following output:

3
4
3
4
The wrap function takes another function which takes two parameters, and returns a new function. The returned function forwards its two parameters to the wrapped function, 
calls it, prints the result, and then returns this result. Implement the wrap function below.
wrap(func) {

///////////////ANSWER////////////////////
function wrap(func) {
return function(x, y) {
	let temp = func(x, y);
	console.log(temp);
	return temp;
	}
}




